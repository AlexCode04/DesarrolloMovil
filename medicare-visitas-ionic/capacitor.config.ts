import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.medicare.visitas',
  appName: 'medicare-visitas-ionic',
  webDir: 'dist'
};

export default config;
